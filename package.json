{
  "name": "esbuild-remark",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Luis Lobo",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "build": "esbuild src/index.js --format=iife --bundle --minify --target=es2020 --outfile=dist/remarkjs.min.js",
    "build_esm": "esbuild src/index.mjs --format=esm --bundle --minify --target=es2020 --outfile=dist/remarkjs.min.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "rehype-sanitize": "^6.0.0",
    "rehype-stringify": "^10.0.1",
    "remark": "^15.0.1",
    "remark-parse": "^11.0.0",
    "remark-rehype": "^11.1.2",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
